# コード解説：タスク1と2

## はじめに

このドキュメントでは、`やること.md`の最初の2つのタスクについて解説します。

- **タスク1**: メインループの特定と理解
- **タスク2**: モデルの訓練構成の確認

---

## タスク1：メインループの特定と理解

### メインファイルはどこ？

```
surv_epn/
└── main/
    └── loops_MLPclinmodel_main.py   ← これがメインファイル
```

### ループの全体像

このコードには **4重のループ（入れ子構造）** があります。
ロシアのマトリョーシカ人形のように、ループの中にループが入っています。

```
[外側] テストセット分割ループ (5回繰り返し)
  └── [2段目] 学習ループ (10回繰り返し)
        └── [3段目] グリッドサーチループ
              └── [内側] エポックループ ← ここが実際の訓練！
```

### 各ループの役割

#### 1. テストセット分割ループ（一番外側）

```python
# loops_MLPclinmodel_main.py 188行目
for test_s in range(n_test_splits):  # n_test_splits = 5
```

**役割**: データをランダムに訓練用・テスト用に分けることを5回繰り返す

**なぜ必要？**: 「たまたま都合のよい分け方になった」という偶然を避けるため。
5回分けて、5回の結果を平均することで、より信頼性の高い評価ができる。

#### 2. 学習ループ（2番目）

```python
# loops_MLPclinmodel_main.py 216行目
for l in range(_parameters.n_loops):  # n_loops = 10
```

**役割**: 訓練データをさらに「訓練用」と「検証用」に分けることを10回繰り返す

**なぜ必要？**: 同じくランダム性の影響を減らすため。

#### 3. グリッドサーチループ（3番目）

```python
# loops_MLPclinmodel_main.py 281行目
for loop_gridsearch in range(n_loops_gsearch):
```

**役割**: 「ハイパーパラメータ」の異なる組み合わせを試す

**ハイパーパラメータとは？**: 人間が事前に設定する設定値のこと。例えば：

- 学習率（どれくらい速く学習するか）
- 隠れ層のサイズ（モデルの「賢さ」）
- バッチサイズ（一度に何個のデータを処理するか）

#### 4. エポックループ（内側・一番重要！）

```python
# loops_MLPclinmodel_main.py 624行目
for epoch in range(grid_param[config_gridsearch]["nb_epochs"]):
```

**役割**: ここが実際にニューラルネットワークが学習する部分！

1つのエポック = 全訓練データを1回見て、モデルのパラメータを更新すること

---

### 実際の訓練の流れ（エポックループの中身）

各エポックで以下のステップが実行されます：

```
① 訓練（Training）
   → train_val_test(mode="train", ...) を呼び出す
   → データを見てモデルの重みを更新する（学習）

② 検証（Validation）
   → train_val_test(mode="val", ...) を呼び出す
   → 別のデータでモデルの性能を確認する（学習しない）
   → 過学習（overfitting）していないかをチェック

③ 早期停止のチェック（Early Stopping）
   → 検証スコアが改善しなくなったら、早めに学習を止める
   → 無駄な計算を省き、過学習を防ぐ
```

#### 「訓練」と「検証」の違い

|            | 訓練 (Training)    | 検証 (Validation)        |
| ---------- | ------------------ | ------------------------ |
| 目的       | モデルに学習させる | 学習の進み具合を確認     |
| 重みの更新 | **する**     | **しない**         |
| データ     | 訓練データ         | 検証データ（別のデータ） |

```python
# 訓練の呼び出し（629行目付近）
roc_auc_train, train_loss, ... = train_val_test(
    model_type,
    "train",          # ← "train"を指定すると重みが更新される
    train_loader,
    model,
    criterion,
    m,
    optimizer,
    ...
)

# 検証の呼び出し（649行目付近）
roc_auc_val, val_loss, ... = train_val_test(
    model_type,
    "val",            # ← "val"を指定すると重みは更新されない
    validation_loader,
    model,
    criterion,
    m,
    data=val_data_split
)
```

#### 全エポック終了後のテスト

```python
# 全エポックが終わったら（697行目付近）
model = torch.load(f"./save/best_model_{model_type}.pth")  # 最良モデルを読み込む
roc_auc_test, test_loss, ... = train_val_test(
    model_type,
    "test",           # ← テストデータで最終評価
    test_loader,
    model,
    ...
)
```

---

### データの分割の仕方（図解）

```
全患者データ
├── テストデータ (10%) → 最終評価用（絶対に訓練に使わない！）
└── 残りのデータ (90%)
    ├── 検証データ (10%) → 学習中の進み具合を確認
    └── 訓練データ (80%) → 実際にモデルに学習させる
```

```python
# parameters.py の設定
self.prop_val = 0.10   # 検証データの割合: 10%
self.prop_test = 0.10  # テストデータの割合: 10%
```

---

## タスク2：モデルの訓練構成の確認

### このプロジェクトで使われるモデルの種類

`surv_epn/functions/models.py` と `surv_epn/functions/epn.py` にモデルが定義されています。

#### モデルの一覧

| モデル名 | 説明                                               |
| -------- | -------------------------------------------------- |
| MLP      | 多層パーセプトロン（普通のニューラルネットワーク） |
| GNN      | グラフニューラルネットワーク                       |
| GAT      | グラフアテンションネットワーク                     |
| MILModel | Multiple Instance Learning モデル                  |
| EPN      | Event Propagation Network（患者間の関係を考慮）    |

#### どのモデルを使うかは parameters.py で設定

```python
# parameters.py 6行目
self.model = 'RSF_METABRIC'  # ← ここを変えてモデルを選ぶ
```

---

### MLPモデルの構造（最も基本的なモデル）

`surv_epn/functions/models.py` の44行目から定義されています。

```
入力データ（患者の特徴量）
    ↓
[fc1] 入力層 → 隠れ層
    ↓  (ReLU活性化関数)
[fc2] 隠れ層 → 隠れ層  ← MLP2lay=False のときだけ存在
    ↓  (ReLU活性化関数)
[fc3] 隠れ層 → 出力層
    ↓
予測値（生存予測）
```

```python
# models.py 44〜88行目
class MLP(torch.nn.Module):
    def __init__(self, hidden_channels, input_size, MLP2lay, ...):
        self.fc1 = torch.nn.Linear(self.input_size, self.hidden_size)   # 第1層
        self.relu = torch.nn.ReLU()                                      # 活性化関数
        if(not MLP2lay):
            self.fc2 = torch.nn.Linear(self.hidden_size, self.hidden_size)  # 第2層（任意）
        self.fc3 = torch.nn.Linear(self.hidden_size, output_size)       # 出力層
```

**「各パーツが別々に訓練されているか？」** → **NO（別々ではない）**

MLPの各層（fc1, fc2, fc3）は、**ひとまとめに（同時に）** 訓練されます。
`optimizer.step()` が呼ばれると、全層の重みが一度に更新されます。

---

### MLPとEPNの関係（重要！）

**MLPとEPNは「別々に」訓練されますが、「並行して」ではありません。**

2段階の訓練プロセスになっています：

```
【ステップ1】 MLPの訓練
loops_MLPclinmodel_main.py を実行

    → MLPモデルが学習される
    → 全患者について「生存予測値」を計算
    → その結果をCSVファイルに保存（433行目付近）

              ↓ （別のスクリプトで後日実行）

【ステップ2】 EPNの訓練
epn.py を使って実行

    → 保存されたCSVファイルを読み込む
    → MLPの予測値 + 患者の特徴量 を入力として使う
    → 患者間のグラフ（ネットワーク）を構築
    → EPNモデルが学習される
```

```python
# main scriptの中でCSVに保存している部分（433行目付近）
surv.to_csv(
    f'./save/results/..../res_EPN_full_tloop_{test_s+1}_loop_{l+1}.csv',
    index=False
)
# ↑ このファイルをEPNが後で読み込む
```

**なぜ2段階に分けるの？**

MLP（標準的なニューラルネット）で個々の患者の基本的な生存予測を行い、
その後EPN（グラフを使ったモデル）で「似た患者同士の情報」を活用して
予測を洗練させるという設計になっています。

---

### generate_model()関数がモデルを生成する仕組み

`surv_epn/functions/loops_functions.py` の570行目に定義されています。

```python
# loops_functions.py 570行目
def generate_model(model_type, hidden_channels, ...):
    if(model_type == 'RSF_METABRIC'):
        model = RandomSurvivalForest(...)      # ランダムサバイバルフォレスト
    elif(model_type in MLP_list):
        model = MLP(hidden_channels, ...)      # 多層パーセプトロン
    elif(model_type in MIL_list):
        model = MILModel(prepNN, afterNN, ...) # MILモデル
    elif(model_type == 'GraphConv_img'):
        model = GNN(hidden_channels, ...)      # グラフニューラルネット
    elif(model_type == 'GAT_img'):
        model = GAT(hidden_channels, ...)      # グラフアテンション
    return model
```

`parameters.py` の `self.model` の値によって、どのモデルを生成するかが決まります。

---

## まとめ

### タスク1のまとめ（メインループ）

```
メインループ構造（loops_MLPclinmodel_main.py）:

for test_s in range(5):           # 異なるテスト分割で5回
    データをtrain/val/testに分割
    for l in range(10):           # 異なるval分割で10回
        for loop_gs in range(N):  # ハイパーパラメータのN通り
            モデルを初期化 (generate_model)
            for epoch in range(nb_epochs):  # 実際の訓練
                ① 訓練 → 重みを更新
                ② 検証 → 重みは更新しない
                ③ 早期停止チェック
            テスト → 最終評価
```

### タスク2のまとめ（モデルの訓練構成）

- **MLP内部の各層（fc1, fc2, fc3）**: 同時に（一緒に）訓練される
- **MLPとEPN**: 別々に訓練される（パイプライン型）
  - まずMLPを訓練 → 予測をCSVに保存
  - 次にEPNを訓練 → MLPの予測を入力として使う

---

## 参照ファイル

| 役割                          | ファイル                                                                          |
| ----------------------------- | --------------------------------------------------------------------------------- |
| メインスクリプト（一番重要）  | [surv_epn/main/loops_MLPclinmodel_main.py](surv_epn/main/loops_MLPclinmodel_main.py) |
| モデルの定義（MLP, GNN, GAT） | [surv_epn/functions/models.py](surv_epn/functions/models.py)                         |
| EPNモデルの定義               | [surv_epn/functions/epn.py](surv_epn/functions/epn.py)                               |
| 訓練・検証・テスト関数        | [surv_epn/functions/loops_functions.py](surv_epn/functions/loops_functions.py)       |
| パラメータ設定                | [surv_epn/main/parameters.py](surv_epn/main/parameters.py)                           |
