# やったこと

## 今回実装した内容：複数スプリット実験ラッパー

---

## 作成したファイル

```
Vanlentin's pipeline/src/survival_analysis/experiments/run_all_splits.py
```

---

## このファイルは何をするもの？

### 背景（なぜ必要か）

彼女のオリジナルコード（`surv_epn`）は、実験の信頼性を高めるために
**「異なるデータ分割で5回実験して、その平均値を報告する」** という方法を使っていました。

しかしValentinのパイプライン（既存の `evaluate_dgm.py`）は、
**1回だけしか実験しない**設計でした。

1回だけの実験では、「たまたまデータの分け方が良かった（or悪かった）」という
偶然の影響を受けてしまいます。

```
【問題のある評価】（これが以前のValentinのパイプライン）
  実験 1回 → C-index: 0.68
  → これが本当に良いのか悪いのか判断しにくい

【信頼できる評価】（今回実装したもの）
  実験 1回目 → C-index: 0.68
  実験 2回目 → C-index: 0.71
  実験 3回目 → C-index: 0.65
  実験 4回目 → C-index: 0.69
  実験 5回目 → C-index: 0.70
  → 平均: 0.686 ± 0.021  ← この数値が論文に書けるもの
```

---

## 実装した内容の詳細

### 全体の流れ

```
[開始]
  ↓
splits.json を読み込む（5つのデータ分割が入っている）
  ↓
スプリット1を使って実験（訓練 → 評価）
  ↓
スプリット2を使って実験（訓練 → 評価）
  ↓
スプリット3を使って実験（訓練 → 評価）
  ↓
スプリット4を使って実験（訓練 → 評価）
  ↓
スプリット5を使って実験（訓練 → 評価）
  ↓
5回の結果を集計して平均・標準偏差を計算
  ↓
results_all_splits.csv に保存
[終了]
```

### 各スプリットで何が起きるか（1回の実験の詳細）

```
① DataModule を作る
   → 「このデータ分割（splits.json の n番目）を使う」という設定

② SurvivalDGM モデルを初期化する
   → Valentinのメインモデル。グラフ構造を自動学習する

③ 訓練する（110エポック）
   → Lightning の Trainer が自動的にやってくれる

④ 評価する（100回の確率的サンプリング）
   → グラフがランダムなので、100回評価して平均を取る
   → C-index（高いほど良い）と IBS（低いほど良い）を計算
```

---

## コードの中で重要なポイント

### ポイント1：`partial` を使ったオプティマイザの渡し方

```python
# ❌ こう書きたくなるけど、SurvivalDGM は受け付けない
optimizer = torch.optim.Adam(model.parameters(), lr=0.001)
model = SurvivalDGM(..., optimizer=optimizer)

# ✅ 正しい書き方：「Adamを使う設定」だけを先に渡す
from functools import partial
optimizer_fn = partial(torch.optim.Adam, lr=0.001, weight_decay=5e-4)
model = SurvivalDGM(..., optimizer=optimizer_fn)
```

**なぜこうするの？**
モデルを初期化するときには、まだモデルのパラメータが確定していません。
`partial` を使うと「後でパラメータを渡してAdamを作る関数」として
定義できます。SurvivalDGM の内部でパラメータが決まってから
オプティマイザを作る仕組みになっています。

### ポイント2：`CoxPH` ラッパーで評価する

```python
# 訓練済みの SurvivalDGM を CoxPH でラップする
survival_model = CoxPH(model)

# 既存の evaluate() 関数に渡す
mean_cindex, mean_brier, std_cindex = evaluate(datamodule, survival_model, nb_tests=100)
```

**なぜ CoxPH でラップするの？**
PyCox ライブラリの評価関数を使うために、モデルを
`CoxPH` という「CoxPH 損失関数を使うモデル」のラッパーに入れる必要があります。
`evaluate()` 関数は `survival_model.predict_surv_df()` を呼び出しますが、
これは `CoxPH` オブジェクトのメソッドです。

### ポイント3：評価を100回繰り返す理由

```python
evaluate(datamodule, survival_model, nb_tests=100)
```

SurvivalDGM は「グラフ構造を確率的にサンプリングする」モデルです。
つまり、同じデータを入れても毎回少し違うグラフが作られます。

```
1回目の評価 → グラフA → C-index: 0.71
2回目の評価 → グラフB → C-index: 0.68
3回目の評価 → グラフC → C-index: 0.70
...
100回の評価 → 平均: 0.695
```

100回評価して平均を取ることで、「たまたまいいグラフが引けた」という
偶然の影響を排除できます。

---

## 実行方法

```bash
cd "Vanlentin's pipeline"
python -m survival_analysis.experiments.run_all_splits
```

実行すると：
1. ターミナルに各スプリットの結果が表示される
2. 全スプリット終了後に平均・標準偏差が表示される
3. `Vanlentin's pipeline/results_all_splits.csv` に結果が保存される

### 出力ファイルの形式（results_all_splits.csv）

| split_index | c_index | ibs  | c_index_std_within_split |
|-------------|---------|------|--------------------------|
| 0           | 0.6800  | 0.18 | 0.012                    |
| 1           | 0.7100  | 0.17 | 0.015                    |
| 2           | 0.6500  | 0.19 | 0.011                    |
| 3           | 0.6900  | 0.18 | 0.013                    |
| 4           | 0.7000  | 0.17 | 0.014                    |
| 全体平均    | 0.6860  | 0.18 | 0.013                    |
| 標準偏差    | 0.0200  | 0.01 | 0.001                    |

- `c_index`: C-index の平均値（高いほど良い。0.5=ランダム、1.0=完璧）
- `ibs`: Integrated Brier Score（低いほど良い）
- `c_index_std_within_split`: 100回の確率的評価内での標準偏差

---

## 既存コードとの関係

このスクリプトは、既存のコードを **再利用** して作られています。
新しいアルゴリズムは一切追加していません。

```
run_all_splits.py（新規）
    ↓ 使う
MetabricGraphSurvivalDataModule  ← 既存のデータ読み込みクラス
    ↓ 使う
SurvivalDGM                      ← 既存のモデル
    ↓ 使う
evaluate()                       ← evaluate_dgm.py の既存の評価関数
```

---

## 次の作業（残っているもの）

| # | 作業 | 難易度 | 優先度 |
|---|------|--------|--------|
| ② | EPNモデルの移植 | 高 | 高 |
| ③ | DeepHitS損失関数の追加 | 中 | 低 |

EPNの移植が最も重要で最も難しい残り作業です。
詳細は [コード解説_タスク4_統合案.md](コード解説_タスク4_統合案.md) を参照してください。
