# やったこと

---

## ✅ 作業④：EPNモデルのLightningへの統合

### 作成したファイル

```
Vanlentin's pipeline/src/survival_analysis/data/epn_datamodule.py   ← EPN用データ準備
Vanlentin's pipeline/src/survival_analysis/models/epn_module.py      ← EPNモデル本体
Vanlentin's pipeline/src/survival_analysis/experiments/run_epn.py    ← 実行スクリプト
```

> `surv_epn/` フォルダは **参照用** としてワークスペースに置いているだけで、
> パイプラインの実行時には一切 import されません。

---

### 設計方針

```
【方針】
  surv_epn/ は「参考書」として隣に置く。
  Valentin のパイプライン（epn_module.py）にロジックをゼロから書き起こす。
  → どちらのコードも独立して動く状態にする。

【NG（やっていないこと）】
  sys.path.insert(0, ".../surv_epn")  ← NG
  from functions.epn import EPN_surv  ← NG
```

---

### EPNとは何か（Error Propagation Network）

EPNは「似た患者の予測誤差を借りて、自分の予測を修正する」モデルです。

```
【普通のMLP予測】
患者A の特徴量 → MLP → 生存曲線（例: 0.8 → 0.6 → 0.3 ...）

【EPN補正後】
患者A と似た患者B, C の予測誤差を計算:
  患者B: 「MLPは生存率を低く予測しすぎた（誤差: +0.1）」
  患者C: 「MLPは生存率を高く予測しすぎた（誤差: -0.05）」

→ 患者BとCの誤差を、類似度（アテンション）で重みつけ平均
→ 患者A の予測を補正: 0.80 → 0.82 ...
```

「アテンション」とは「どの患者をどれくらい参考にするか」の重みで、
EPNはこれを自動的に学習します。

---

### 2段階訓練の仕組み

```
【第1段階】 MLP を 110 エポック訓練する
               ↓
           MLP のパラメータを凍結（mlp.freeze()）
           → 以降、MLP は更新されない

【第2段階】 EPN を 50 エポック訓練する
           入力: MLPが出力した全患者の生存曲線（事前計算済み）
           学習: どの患者の誤差をどれくらい参考にするか（アテンション）
```

---

### ファイル構成と役割

#### `epn_datamodule.py` — データ準備

MLP の生存曲線予測をすべての患者に対して事前計算し、
EPN が必要とする形式の pandas DataFrame を作ります。

```
DataFrame の列構造:
  pat_id, feat_0 ... feat_8  ← 患者特徴量（9列）
  "0.0", "5.0", "10.0", ...  ← MLPが予測した生存確率（時点ごと、約1391列）
  label_duration, label_event, split, pat_to_compute, batch
```

訓練時と検証時で `pat_to_compute` フラグの使い方が異なります：

```
【訓練モード】train_df:
  訓練患者のみ（約1523人）を含む
  毎ステップ 256人をランダムに選んで pat_to_compute=True にする
  → 選ばれた256人が、残り全員を参考にして学習

【検証モード】val_df:
  全患者（約1904人）を含む
  訓練患者: pat_to_compute=False  ← 隣人候補（参照のみ）
  検証患者: pat_to_compute=True   ← 補正・評価の対象
```

#### `epn_module.py` — EPN モデル本体（完全独立実装）

EPN のすべてのロジックをこのファイルに実装しています：

| クラス/関数 | 内容 |
|---|---|
| `EPNModel` | EPN のコア（`nn.Module`） |
| `→ forward(df)` | DataFrame を受け取り、補正済み生存曲線を返す |
| `→ loss(...)` | CoxCC 損失 + 正則化 |
| `→ _compute_error_survival(...)` | 予測と正解ラベルの誤差を計算 |
| `→ _make_case_control(...)` | CoxCC 損失用のケース・コントロール作成 |
| `EPNModule` | Lightning ラッパー（訓練ループの自動化） |

#### `run_epn.py` — 実行スクリプト

```
①  MLP を訓練（110エポック）
②  MLP を凍結
③  EPNDataModule を初期化（生存曲線を事前計算）
④  EPN を訓練（50エポック）
⑤  C-index と IBS を計算して保存
```

---

### 実装上のポイント：メモリ問題とミニバッチ化

EPN のアテンション計算では中間テンソルとして
**[患者数 × 患者数 × 時点数]** の行列が作られます。

全訓練患者（1523人）を一度に処理しようとすると：
```
1523 × 1523 × 1391 × 4バイト ≈ 12.9 GB  → メモリ不足でクラッシュ ❌
```

解決策：ミニバッチ化して 1 ステップあたり 256 人だけ処理する：
```
256 × 256 × 1391 × 4バイト ≈ 363 MB   → 問題なし ✅
```

`MiniBatchEPNDataset` が毎ステップ 256 人をランダムに選び、
残りの患者は「隣人候補」として DataFrame に残したまま損失計算から外します。

---

### 実行結果（全5スプリット）

```bash
cd "Vanlentin's pipeline"
conda run -n graph_survival python -m survival_analysis.experiments.run_epn
```

| スプリット | C-index | IBS |
|---|---|---|
| 1 | 0.6452 | 0.1741 |
| 2 | 0.5856 | 0.1604 |
| 3 | 0.6099 | 0.1831 |
| 4 | 0.5972 | 0.1732 |
| 5 | 0.5867 | 0.1701 |
| **平均** | **0.6049 ± 0.0219** | **0.1722 ± 0.0073** |

---

### 3モデルの比較

| モデル | Mean C-index | Mean IBS |
|---|---|---|
| SurvivalDGM（Valentinのモデル） | 0.6043 ± 0.0244 | 0.1754 |
| MLP（彼女のモデル） | 0.6495 ± 0.0097 | 0.1664 |
| **EPN（今回統合）** | **0.6049 ± 0.0219** | **0.1722** |

**考察：**
EPN は今回の設定では MLP より C-index が低い結果でした。
ただしハイパーパラメータ（エポック数・隣人の割合など）はまだ未最適化です。
今回の目標は「**Valentin のパイプライン単独で EPN を動かすこと**」であり、
MLP 訓練 → 凍結 → EPN 訓練 → 評価の全パイプラインが正常動作しています ✅

---

## ✅ 作業③：患者類似グラフ構築関数の実装

### 作成したファイル

```
Vanlentin's pipeline/src/survival_analysis/data/graph_builder.py
```

---

### なぜグラフ構築関数が必要か

指導教員からの指示：
> 「EPNのような誤差伝播モデルを作る前に、まず独立したグラフ構築関数を作りなさい。NetworkXではなくPyTorch Geometricを使うこと。」

EPNの核心は「似た患者から学ぶ」こと。そのために患者同士を繋ぐグラフが必要です。

---

### k-NN グラフとは

「各患者の最も近い k 人に線を引く」方法です。

```python
# コサイン類似度で k=10 の患者類似グラフを構築
graph = build_patient_similarity_graph(patient_features, k=10, metric='cosine')
```

---

### Oriane（EPN_surv）との違い

| | 今回の関数 | EPN_surv の内部処理 |
|---|---|---|
| グラフの種類 | 明示的（事前に構築） | 暗黙的（アテンションで学習） |
| エッジ | k-NNで固定 | 学習により変動 |
| フレームワーク | PyTorch Geometric | 生の PyTorch |

---

### テスト結果（動作確認）

**シンプルなケース（5患者）：**

```
患者0: [1.0, 0.0] → 隣人: 患者1, 患者2 ✅
患者3: [0.0, 1.0] → 隣人: 患者4, 患者2 ✅ (期待通り)
```

**METABRICデータ（訓練用 1523人）：**

```
ノード数: 1523
エッジ数: 15230（= 1523 × 10）
各患者が正確に 10 人の隣人を持っている ✅
```

---

## ✅ 作業②：彼女のMLPをValentinのパイプラインに組み込む

### 作成したファイル

```
Vanlentin's pipeline/src/survival_analysis/models/mlp_module.py   ← MLPモデル本体
Vanlentin's pipeline/src/survival_analysis/experiments/run_mlp.py ← 実行スクリプト
```

---

### なぜMLPの組み込みが必要なのか

彼女のパイプラインは「2段階訓練」の設計になっています。

```
【第1段階】 MLP（普通のニューラルネット）を訓練する
               ↓
【第2段階】 EPN（グラフを使ったモデル）を訓練する
           （MLPの予測値を入力として使う）
```

EPNを動かすためには、**まずMLPが動かなければいけません**。
今回はこの第1段階を完成させました。

---

### MLPとは何か（復習）

MLP（Multi-Layer Perceptron）は「多層パーセプトロン」とも呼ばれる、
最も基本的なニューラルネットワークです。

```
患者の特徴量（9個の数値）
    ↓  [fc1] 入力層 → 隠れ層（64ユニット）+ ReLU
    ↓  [fc2] 隠れ層 → 隠れ層（64ユニット）+ ReLU
    ↓  [fc3] 隠れ層 → 出力層（1ユニット）
生存リスクスコア（1つの数値）
```

**ReLU**とは「負の値を0にする」という変換で、
ニューラルネットが複雑なパターンを学べるようにする仕組みです。

---

### PyTorch Lightningでのラップとは何か

彼女のオリジナルコード（`surv_epn/functions/models.py`）のMLPは
**生のPyTorchモジュール**として書かれています。

Valentinのパイプラインは**PyTorch Lightning**というフレームワークを使っており、
訓練・検証・オプティマイザの設定をひとまとめにした「LightningModule」が必要です。

```python
# 彼女のオリジナルコード（生のPyTorch）
class MLP(torch.nn.Module):
    def forward(self, x): ...     # 予測するだけ
    # 訓練のループは別ファイルに書かれている

# 今回作ったコード（PyTorch Lightning）
class MLPModule(pl.LightningModule):
    def forward(self, x): ...            # 予測
    def training_step(self, batch): ...  # 訓練の1ステップ
    def validation_step(self, batch):... # 検証の1ステップ
    def configure_optimizers(self): ...  # 最適化アルゴリズムの設定
```

Lightningにすることで、`trainer.fit(model, datamodule)` の1行で
訓練・検証・エポック管理がすべて自動化されます。

---

### CoxPH損失関数とは

「損失関数」とは、モデルの予測がどれだけ間違っているかを数値化するものです。

CoxPH（Cox Proportional Hazards）は**生存分析専用の損失関数**です。

- 普通の損失関数（例：平均二乗誤差）は「予測値と正解の差」を測る
- CoxPHは「死亡した患者を、まだ生存している患者より高リスクと予測できているか」を測る

打ち切り（censored）データ、つまり「観察終了時点でまだ生きていた患者」も
正しく扱えるのが生存分析専用損失関数の利点です。

---

### 実行結果

```bash
cd "Vanlentin's pipeline"
conda run -n graph_survival python -m survival_analysis.experiments.run_mlp
```

| スプリット | C-index | IBS |
|---|---|---|
| 1 | 0.6552 | 0.1727 |
| 2 | 0.6494 | 0.1550 |
| 3 | 0.6348 | 0.1780 |
| 4 | 0.6634 | 0.1680 |
| 5 | 0.6444 | 0.1583 |
| **平均** | **0.6495 ± 0.0097** | **0.1664 ± 0.0086** |

**C-index の標準偏差が 0.0000 になっている理由：**
MLPはランダム性のないモデルなので、同じデータを渡せば毎回まったく同じ予測をします。
そのため100回評価しても結果が変わらず、標準偏差が0になります。
（SurvivalDGMはグラフを確率的にサンプリングするので、100回評価して平均を取る必要がありました）

---

### SurvivalDGMとの比較

| モデル | Mean C-index | Mean IBS |
|---|---|---|
| SurvivalDGM（Valentinのモデル） | 0.6043 ± 0.0244 | 0.1754 |
| **MLP（彼女のモデル）** | **0.6495 ± 0.0097** | **0.1664** |

MLPの方がC-indexが高く、IBSが低い（どちらも良い方向）という結果になりました。
これはMLPがシンプルなデータ（METABRICの臨床データ）に対して十分に機能することを示しています。
SurvivalDGMはグラフ構造を学習するより複雑なモデルですが、
グラフが本当に有益かどうかはデータによって変わります。

---

### 次の作業

MLPが動いたので、次はいよいよ **EPN（第2段階モデル）の組み込み** です。

```
✅ 第1段階：MLP の組み込み（今回完了）
→ 第2段階：EPN の組み込み（次回）
           MLPの予測値を使ってEPNを訓練する
           MLPのパラメータは固定する（更新させない）
```

---

## ✅ 作業①：複数スプリット実験ラッパーの実装（前回完了）

## 今回実装した内容：複数スプリット実験ラッパー

---

## 作成したファイル

```
Vanlentin's pipeline/src/survival_analysis/experiments/run_all_splits.py
```

---

## このファイルは何をするもの？

### 背景（なぜ必要か）

彼女のオリジナルコード（`surv_epn`）は、実験の信頼性を高めるために
**「異なるデータ分割で5回実験して、その平均値を報告する」** という方法を使っていました。

しかしValentinのパイプライン（既存の `evaluate_dgm.py`）は、
**1回だけしか実験しない**設計でした。

1回だけの実験では、「たまたまデータの分け方が良かった（or悪かった）」という
偶然の影響を受けてしまいます。

```
【問題のある評価】（これが以前のValentinのパイプライン）
  実験 1回 → C-index: 0.68
  → これが本当に良いのか悪いのか判断しにくい

【信頼できる評価】（今回実装したもの）
  実験 1回目 → C-index: 0.68
  実験 2回目 → C-index: 0.71
  実験 3回目 → C-index: 0.65
  実験 4回目 → C-index: 0.69
  実験 5回目 → C-index: 0.70
  → 平均: 0.686 ± 0.021  ← この数値が論文に書けるもの
```

---

## 実装した内容の詳細

### 全体の流れ

```
[開始]
  ↓
splits.json を読み込む（5つのデータ分割が入っている）
  ↓
スプリット1を使って実験（訓練 → 評価）
  ↓
スプリット2を使って実験（訓練 → 評価）
  ↓
スプリット3を使って実験（訓練 → 評価）
  ↓
スプリット4を使って実験（訓練 → 評価）
  ↓
スプリット5を使って実験（訓練 → 評価）
  ↓
5回の結果を集計して平均・標準偏差を計算
  ↓
results_all_splits.csv に保存
[終了]
```

### 各スプリットで何が起きるか（1回の実験の詳細）

```
① DataModule を作る
   → 「このデータ分割（splits.json の n番目）を使う」という設定

② SurvivalDGM モデルを初期化する
   → Valentinのメインモデル。グラフ構造を自動学習する

③ 訓練する（110エポック）
   → Lightning の Trainer が自動的にやってくれる

④ 評価する（100回の確率的サンプリング）
   → グラフがランダムなので、100回評価して平均を取る
   → C-index（高いほど良い）と IBS（低いほど良い）を計算
```

---

## コードの中で重要なポイント

### ポイント1：`partial` を使ったオプティマイザの渡し方

```python
# ❌ こう書きたくなるけど、SurvivalDGM は受け付けない
optimizer = torch.optim.Adam(model.parameters(), lr=0.001)
model = SurvivalDGM(..., optimizer=optimizer)

# ✅ 正しい書き方：「Adamを使う設定」だけを先に渡す
from functools import partial
optimizer_fn = partial(torch.optim.Adam, lr=0.001, weight_decay=5e-4)
model = SurvivalDGM(..., optimizer=optimizer_fn)
```

**なぜこうするの？**
モデルを初期化するときには、まだモデルのパラメータが確定していません。
`partial` を使うと「後でパラメータを渡してAdamを作る関数」として
定義できます。SurvivalDGM の内部でパラメータが決まってから
オプティマイザを作る仕組みになっています。

### ポイント2：`CoxPH` ラッパーで評価する

```python
# 訓練済みの SurvivalDGM を CoxPH でラップする
survival_model = CoxPH(model)

# 既存の evaluate() 関数に渡す
mean_cindex, mean_brier, std_cindex = evaluate(datamodule, survival_model, nb_tests=100)
```

**なぜ CoxPH でラップするの？**
PyCox ライブラリの評価関数を使うために、モデルを
`CoxPH` という「CoxPH 損失関数を使うモデル」のラッパーに入れる必要があります。
`evaluate()` 関数は `survival_model.predict_surv_df()` を呼び出しますが、
これは `CoxPH` オブジェクトのメソッドです。

### ポイント3：評価を100回繰り返す理由

```python
evaluate(datamodule, survival_model, nb_tests=100)
```

SurvivalDGM は「グラフ構造を確率的にサンプリングする」モデルです。
つまり、同じデータを入れても毎回少し違うグラフが作られます。

```
1回目の評価 → グラフA → C-index: 0.71
2回目の評価 → グラフB → C-index: 0.68
3回目の評価 → グラフC → C-index: 0.70
...
100回の評価 → 平均: 0.695
```

100回評価して平均を取ることで、「たまたまいいグラフが引けた」という
偶然の影響を排除できます。

---

## 実行方法

```bash
cd "Vanlentin's pipeline"
python -m survival_analysis.experiments.run_all_splits
```

実行すると：
1. ターミナルに各スプリットの結果が表示される
2. 全スプリット終了後に平均・標準偏差が表示される
3. `Vanlentin's pipeline/results_all_splits.csv` に結果が保存される

### 出力ファイルの形式（results_all_splits.csv）

| split_index | c_index | ibs  | c_index_std_within_split |
|-------------|---------|------|--------------------------|
| 0           | 0.6800  | 0.18 | 0.012                    |
| 1           | 0.7100  | 0.17 | 0.015                    |
| 2           | 0.6500  | 0.19 | 0.011                    |
| 3           | 0.6900  | 0.18 | 0.013                    |
| 4           | 0.7000  | 0.17 | 0.014                    |
| 全体平均    | 0.6860  | 0.18 | 0.013                    |
| 標準偏差    | 0.0200  | 0.01 | 0.001                    |

- `c_index`: C-index の平均値（高いほど良い。0.5=ランダム、1.0=完璧）
- `ibs`: Integrated Brier Score（低いほど良い）
- `c_index_std_within_split`: 100回の確率的評価内での標準偏差

---

## 既存コードとの関係

このスクリプトは、既存のコードを **再利用** して作られています。
新しいアルゴリズムは一切追加していません。

```
run_all_splits.py（新規）
    ↓ 使う
MetabricGraphSurvivalDataModule  ← 既存のデータ読み込みクラス
    ↓ 使う
SurvivalDGM                      ← 既存のモデル
    ↓ 使う
evaluate()                       ← evaluate_dgm.py の既存の評価関数
```

---

## 次の作業（残っているもの）

| # | 作業 | 難易度 | 優先度 |
|---|------|--------|--------|
| ② | EPNモデルの移植 | 高 | 高 |
| ③ | DeepHitS損失関数の追加 | 中 | 低 |

EPNの移植が最も重要で最も難しい残り作業です。
詳細は [コード解説_タスク4_統合案.md](コード解説_タスク4_統合案.md) を参照してください。
