
**録音内容から、学生が次に取り組むべきタスクは以下の通りです。**

**1. 作成したパイプラインの動作確認（最優先）**

* **学生が新しく作成したパイプラインのスクリプトは良く書けていますが、まずは他のモデルを試す前に、指導者のモデルを使ってコードが正常に動作するか（バグがないか）をテストする必要があります**。

**2. 「彼女」のモデルの組み込みとトレーニング（メインの目標）**

* **別の人物（彼女）が作成したモデルを、学生が構築したパイプライン内に組み込み、トレーニングできるようにすることが今回の最大の目標です**。
* **彼女のモデルは複数のモデル（最初のMLPモデルと、それに依存する2番目のモデルなど）で構成されているため、以下のように実装することが推奨されています**。
  * **PyTorch Lightningを使用する:** コードをより読みやすく、扱いやすくするために推奨されています**。**
  * **最初のモデルを学習する:** まず、最初のモデル（MLPなど）を作成してトレーニングします**。**
  * **2番目のモデルに統合する:** 2番目のモデルを作成する際、事前学習済みの最初のモデルを入力として受け取るようにします**。**
  * **パラメータの固定:** 2番目のモデルを学習する際、PyTorchのデフォルトでは最初のモデルも学習対象になってしまう可能性があるため、最初のモデルのパラメータが更新されないよう（例：`trainable = False`）に設定して確認する必要があります**。**

**3. 評価関数（Evaluation）の書き換え**

* **指導者のモデルと彼女のモデルでは、予測する内容や評価指標（C-indexなど）が異なる可能性があります**。
* **そのため、彼女のモデルに合わせた新しい評価関数を作成し、既存の評価関数と置き換えて実行できるようにする必要があります**。
* **ただし、評価部分の修正は難易度が高いため、** **まずは「モデルをパイプラインに組み込んでトレーニング可能にする」ことだけを達成できれば十分** **（大成功）とされています**。

**4. 作業時の注意点**

* **元のモデルの動作を誤って変えてしまわないように、彼女のモデルのコードを変更する際は、必要最小限に留めるように心がける必要があります**。
* **Hydraへの統合も将来的には必要ですが、今は後回しで構いません**。

**これらのタスクを、次回のミーティング（21日木曜日の午後3時）までに進めることが期待されています**。困難な部分があれば、いつでも指導者に質問して良いとのことです**。**
