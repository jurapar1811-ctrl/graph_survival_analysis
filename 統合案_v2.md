# 統合案 v2 ― ミーティング後の更新版

> **更新日**: 2026-05-19  
> このドキュメントは指導者とのミーティング内容を踏まえて  
> `コード解説_タスク4_統合案.md` を全面的に書き直したものです。

---

## 0. 現状の整理（前回からの変化）

| 作業 | 前回 | 今回 |
|---|---|---|
| 複数スプリット実験ラッパー | 残り | **✅ 完了**（`run_all_splits.py`） |
| パイプライン動作確認 | 未着手 | **★ 最優先** |
| 彼女のMLPの組み込み | 残り | **★ メイン目標** |
| 彼女のEPN_survの組み込み | 残り | **★ メイン目標** |
| 評価関数の修正 | 残り | 後回しでよい |
| Hydra設定ファイルの整備 | 残り | 後回しでよい |

---

## 1. ミーティングで決まった優先順位

```
【最優先】①  パイプラインの動作確認
              → run_all_splits.py を実行してバグがないか確認する

【メイン】②  彼女のMLPをLightningModuleとして組み込む
              → まず1つ目のモデル（MLP）を単独で訓練できるようにする

【メイン】③  彼女のEPN_survをLightningModuleとして組み込む
              → 訓練済みMLPを入力として受け取るように接続する
              → MLPのパラメータは固定する（更新させない）

【後回し】④  評価関数の修正
              → 難しいので、まずは③まで動けば「大成功」
```

---

## 2. Step 1：パイプラインの動作確認（最優先）

### やること

```bash
cd "Vanlentin's pipeline"
python -m survival_analysis.experiments.run_all_splits
```

これが正常に完了すれば、**SurvivalDGM**（Valentinのモデル）が
5スプリットすべてで訓練・評価できていることを意味します。

### 確認すべき項目

- [ ] エラーなく最後まで実行できる
- [ ] `results_all_splits.csv` が生成される
- [ ] C-index が 0.5 以上（0.5 = ランダム予測と同等）
- [ ] IBS が合理的な値（通常 0.1 〜 0.3 程度）

---

## 3. Step 2：彼女のMLPをパイプラインに組み込む

### このステップの目標

彼女のMLPモデル（`surv_epn/functions/models.py` の `MLP` クラス）を
Valentinのスタイル（PyTorch Lightning）で訓練できるようにする。

### 彼女のMLPの構造（復習）

```python
# surv_epn/functions/models.py（44行目付近）
class MLP(torch.nn.Module):
    def __init__(self, hidden_channels, input_size, MLP2lay, ...):
        self.fc1 = Linear(input_size, hidden_size)   # 入力層 → 隠れ層
        self.relu = ReLU()
        if not MLP2lay:
            self.fc2 = Linear(hidden_size, hidden_size)  # 隠れ層 → 隠れ層（任意）
        self.fc3 = Linear(hidden_size, output_size)  # 隠れ層 → 出力層
```

**入力**: 患者の特徴量（METABRICなら9次元）  
**出力**: 生存予測値（時間点ごとのスコア）

### 作成するファイル

```
Vanlentin's pipeline/src/survival_analysis/models/mlp_module.py  ← 新規作成
```

### コードのイメージ

```python
# src/survival_analysis/models/mlp_module.py（新規作成）
import torch
import lightning as pl
from pycox.models.loss import CoxPHLoss

class MLPModule(pl.LightningModule):
    """
    彼女のMLPをPyTorch Lightningでラップしたもの。
    CoxPH損失関数で訓練する。
    """
    def __init__(self, in_dim: int, hidden_dim: int, optimizer):
        super().__init__()
        self.save_hyperparameters(ignore=["optimizer"])
        self._optimizer = optimizer

        # 彼女のMLP構造をそのまま再現
        self.fc1 = torch.nn.Linear(in_dim, hidden_dim)
        self.relu = torch.nn.ReLU()
        self.fc2 = torch.nn.Linear(hidden_dim, hidden_dim)
        self.fc3 = torch.nn.Linear(hidden_dim, 1)  # 出力は1次元（CoxPHの場合）

        self.loss_fn = CoxPHLoss()

    def forward(self, x):
        x = self.relu(self.fc1(x))
        x = self.relu(self.fc2(x))
        return self.fc3(x)

    def training_step(self, batch, batch_idx):
        # batch.x = 患者の特徴量
        # batch.y[:, 0] = 生存期間, batch.y[:, 1] = イベント
        pred = self(batch.x)
        loss = self.loss_fn(pred, batch.y[:, 0], batch.y[:, 1])
        self.log("train_loss", loss)
        return loss

    def configure_optimizers(self):
        return self._optimizer(self.parameters())
```

### データはどこから来るか

**既存の `MetabricGraphSurvivalDataModule` をそのまま使う。**

```
MetabricGraphSurvivalDataModule
    ↓ 提供するもの
  batch.x  = 患者の特徴量テンソル（9次元）
  batch.y  = [生存期間, イベント] テンソル
```

`MLPModule.training_step()` はこの形式をそのまま受け取れる。

---

## 4. Step 3：彼女のEPN_survをパイプラインに組み込む

### このステップの目標（ミーティングの最大の目標）

**「訓練済みMLPを使って、EPNをその上で訓練できるようにする」**

指導者の指示：
- 2つ目のモデル（EPN）を訓練するとき、**1つ目のモデル（MLP）のパラメータを固定する**
- PyTorchのデフォルトでは両方が学習対象になってしまうため、明示的に固定が必要

### 二段階訓練パイプラインの全体像

```
【ステップ①】MLPを訓練する
    DataModule → MLPModule.fit() → 訓練済みMLPの重みを保存

                    ↓ MLPを使って全患者の生存予測値を計算

【ステップ②】EPN_survを訓練する
    入力: 患者の特徴量 + MLPの生存予測値
    MLPのパラメータは固定（trainable = False）
    EPNのパラメータだけが更新される
```

### パラメータ固定の方法

```python
# MLPを読み込んで、パラメータを固定する
mlp = MLPModule.load_from_checkpoint("mlp_checkpoint.ckpt")
mlp.freeze()  # = mlp.requires_grad_(False) と同じ意味

# EPNはMLPを内部に持つ
epn = EPNModule(mlp=mlp, ...)
# EPNを訓練するとき、MLPの重みは更新されない
```

### EPN_survの`forward()`の問題点

彼女のEPN_surv（`surv_epn/functions/epn.py` の326行目）の`forward()`は
**pandasのDataFrame**を受け取る設計になっています。

```python
# 彼女のコード（epn.py 406〜443行目）
def forward(self, input_db):  # ← input_db は pandas DataFrame
    # 列名が時間点の数値: "0.5", "1.2", "2.8", ...  ← MLPの生存予測
    # 列名: "feat_0", "feat_1", ...  ← 患者の特徴量
    # 列名: "label_duration", "label_event"  ← ラベル
    # 列名: "split", "pat_id", "pat_to_compute", "batch"  ← 管理用
    timepoints = sorted(...)  # 列名から時間点を数値として取り出す
    preds = torch.from_numpy(input_db[timepoints].values).float()
    features = torch.from_numpy(input_db[feat_cols].values).float()
```

**この設計のままでは Valentin のパイプラインと接続できません。**

### 解決方法（ミーティングの方針：最小限の変更）

> **指導者の指示**：「元のモデルの動作を誤って変えてしまわないように、  
> 彼女のモデルのコードを変更する際は、必要最小限に留める」

#### 方針：橋渡しDataModuleを作る

EPN_surv自体のコード（`epn.py`）はほぼ変更せず、
**「DataModuleがDataFrameを作って渡す」**という設計にする。

```
Valentinのパイプライン（テンソル世界）
         ↓
   EPNDataModule（橋渡し役）
         ↓  DataFrameを作成して渡す
   EPN_surv（DataFrame世界）
```

#### 作成するファイル一覧

```
Vanlentin's pipeline/src/survival_analysis/
├── models/
│   ├── mlp_module.py        ← 新規（Step 2）
│   └── epn_module.py        ← 新規（Step 3）
└── data/
    └── epn_datamodule.py    ← 新規（Step 3）
```

### EPNDataModuleのイメージ

```python
# src/survival_analysis/data/epn_datamodule.py（新規作成）
import lightning as pl
import pandas as pd
import torch

class EPNDataModule(pl.LightningDataModule):
    """
    訓練済みMLPの予測値と患者特徴量を
    EPN_survが期待するDataFrame形式に変換するDataModule。
    """

    def __init__(self, base_datamodule, trained_mlp):
        super().__init__()
        self.base_dm = base_datamodule   # MetabricGraphSurvivalDataModule
        self.mlp = trained_mlp           # 訓練済みMLPModule（freeze済み）

    def setup(self, stage=None):
        self.base_dm.setup()

        # MLPで全患者の生存予測を計算（パラメータは固定済み）
        self.mlp.eval()
        with torch.no_grad():
            train_preds = self.mlp(self.base_dm.train_graph.x)
            val_preds   = self.mlp(self.base_dm.val_graph.x)

        # EPN_survが期待するDataFrame形式に変換
        self.train_df = self._build_dataframe(
            self.base_dm.train_graph.x,
            train_preds,
            self.base_dm.train_graph.y,
            split="train"
        )
        self.val_df = self._build_dataframe(
            self.base_dm.val_graph.x,
            val_preds,
            self.base_dm.val_graph.y,
            split="val"
        )

    def _build_dataframe(self, features, preds, labels, split):
        """特徴量テンソルとMLPの予測値をDataFrameに変換する。"""
        n = features.shape[0]
        df = pd.DataFrame(
            features.numpy(),
            columns=[f"feat_{i}" for i in range(features.shape[1])]
        )
        # MLPの出力を時間点列として追加（列名は数値文字列）
        df["0.0"] = preds.squeeze().numpy()
        df["label_duration"] = labels[:, 0].numpy()
        df["label_event"]    = labels[:, 1].numpy()
        df["split"]          = split
        df["pat_id"]         = range(n)
        df["pat_to_compute"] = True
        df["batch"]          = True
        df["index"]          = range(n)
        return df
```

### EPNModuleのイメージ

```python
# src/survival_analysis/models/epn_module.py（新規作成）
import lightning as pl
from surv_epn.functions.epn import EPN_surv  # 彼女のコードをそのまま使う

class EPNModule(pl.LightningModule):
    """
    彼女の EPN_surv をPyTorch Lightningでラップしたもの。
    訓練済みMLPを内部に持ち、そのパラメータは固定されている。
    """
    def __init__(self, epn_surv: EPN_surv, optimizer):
        super().__init__()
        self.epn = epn_surv
        self._optimizer = optimizer

    def forward(self, input_df):
        return self.epn(input_df)

    def training_step(self, batch, batch_idx):
        input_df, durations, events = batch
        pred = self(input_df)
        loss = self.epn._criterion(pred, durations, events)
        self.log("train_loss", loss)
        return loss

    def configure_optimizers(self):
        return self._optimizer(self.epn.parameters())
```

---

## 5. Step 4：評価関数の修正（後回しでよい）

### 現状の問題

`evaluate_dgm.py` の `evaluate()` 関数は **SurvivalDGM 専用**の設計です。

```python
# evaluate_dgm.py の evaluate() が前提としている構造
survival_model.compute_baseline_hazards(train_x, ...)  # ← CoxPH専用
surv = survival_model.predict_surv_df(val_x)           # ← CoxPH専用
```

EPN_survはCoxCC損失関数を使っているため、評価方法が異なります。

### 指導者の方針

> 「評価部分の修正は難しいため、**まずはモデルをパイプラインに組み込んで  
> トレーニング可能にすることだけを達成できれば十分（大成功）**」

→ **Step 3（訓練ができる）まで完成すれば、このステップは今回不要。**

---

## 6. ファイル変更・作成の全体マップ

### 新規作成が必要なファイル

| ファイル | 役割 | 難易度 | 対応Step |
|---|---|---|---|
| `src/survival_analysis/models/mlp_module.py` | 彼女のMLPをLightningでラップ | 低 | Step 2 |
| `src/survival_analysis/data/epn_datamodule.py` | MLPの予測値をDataFrameに変換 | 高 | Step 3 |
| `src/survival_analysis/models/epn_module.py` | EPN_survをLightningでラップ | 中 | Step 3 |

### 変更不要（そのまま使う）

| ファイル | 理由 |
|---|---|
| `surv_epn/functions/epn.py` | 指導者の方針：最小限の変更に留める |
| `surv_epn/functions/models.py` | MLPModuleの参考にするが、変更しない |
| `src/survival_analysis/data/metabric_datamodule.py` | Step 2ではそのまま使う |
| `src/survival_analysis/experiments/run_all_splits.py` | Step 1の確認に使う |

### 後回し

| ファイル | 理由 |
|---|---|
| Hydra設定ファイル（configs/） | 指導者が「今は後回しでよい」と明言 |
| `src/survival_analysis/experiments/evaluate_epn.py` | Step 4（難易度が高い） |

---

## 7. 作業の進め方（Step by Step）

```
Step 1（今すぐ）: run_all_splits.py を実行して動作確認
    ↓ バグがなければOK

Step 2（次）: mlp_module.py を作成
    → MetabricGraphSurvivalDataModule のデータで MLPModule を訓練できるか確認

Step 3a（その次）: epn_datamodule.py を作成
    → MLPの予測値を含む DataFrame を作れることを確認

Step 3b: epn_module.py を作成
    → EPN_surv を LightningModule でラップして訓練できるか確認
    → MLP の freeze() が正しく機能しているか確認

Step 4（オプション）: evaluate_epn.py を作成
    → EPN_surv 用の評価関数を書く
    → 次回ミーティング（木曜日午後3時）までには不要
```

---

## 8. 技術的な注意点まとめ

### ① パラメータ固定の確認方法

```python
# MLPを固定した後、本当に固定されているかを確認するコード
mlp.freeze()
for name, param in mlp.named_parameters():
    print(name, param.requires_grad)  # すべて False になるはず
```

### ② 彼女のEPN_survの`forward()`は何を期待しているか

```
入力DataFrameに必要な列:
  - 時間点列（数値文字列）: "0.0", "1.5", "3.0", ... ← MLPの予測値
  - 特徴量列: "feat_0", "feat_1", ...
  - 管理列: "label_duration", "label_event", "split",
            "pat_id", "pat_to_compute", "batch", "index"
```

→ `EPNDataModule._build_dataframe()` でこの形式を作る必要があります。

### ③ 指導者の注意点を守る

- 彼女のコード（`epn.py`, `models.py`）を**直接書き換えない**
- 変更が必要な場合は**Valentinのパイプライン側に橋渡しコードを書く**
- Hydra統合は後回し

---

## 参照ファイル

| 役割 | ファイル |
|---|---|
| 彼女のEPNモデル（移植元） | [surv_epn/functions/epn.py](surv_epn/functions/epn.py)（326行目: `EPN_surv`） |
| 彼女のMLPモデル（移植元） | [surv_epn/functions/models.py](surv_epn/functions/models.py)（44行目: `MLP`） |
| Valentinのモデル（参考） | [Vanlentin's pipeline/src/survival_analysis/models/dgm_model.py](Vanlentin's%20pipeline/src/survival_analysis/models/dgm_model.py) |
| 既存データモジュール（参考） | [Vanlentin's pipeline/src/survival_analysis/data/metabric_datamodule.py](Vanlentin's%20pipeline/src/survival_analysis/data/metabric_datamodule.py) |
| 複数スプリット実験（完成済み） | [Vanlentin's pipeline/src/survival_analysis/experiments/run_all_splits.py](Vanlentin's%20pipeline/src/survival_analysis/experiments/run_all_splits.py) |
| 旧統合案 | [コード解説_タスク4_統合案.md](コード解説_タスク4_統合案.md) |
