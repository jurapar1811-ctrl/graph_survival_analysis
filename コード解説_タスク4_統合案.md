# タスク4：Valentinのパイプラインへの統合案

## はじめに

このドキュメントでは、「彼女のコード（surv_epn）」を
「Valentinのパイプライン」に移植するために
**何が完了していて、何がまだ残っているか**を整理します。

---

## 1. 現状の整理：何がすでに移植済みか

Valentinはすでにパイプラインの骨格を作り、いくつかのモデルを移植しています。

### 移植済み・作成済みのもの

| 彼女のコード（surv_epn） | Valentinのパイプライン | 状態 |
|---|---|---|
| `models.py` の GAT クラス | `models/graph_module.py` の GAT | 移植済み |
| MLPモデル（基本的な構造） | `models/survival_module.py` の SurvModel | 同等のもの作成済み |
| METABRICデータの読み込み | `data/metabric_datamodule.py` | 完成 |
| Gauthierの臨床データの読み込み | `data/gauthier_graph_datamodule.py` | 作成済み |
| （なし）| `models/dgm_model.py` の SurvivalDGM | Valentinの新モデル（彼女のコードにはない） |

加えて、Lightning・Hydra・configファイルなどのフレームワーク全体は
すでに整備されています。

---

## 2. 残っている作業：何がまだ移植されていないか

以下の3つが未完成です。

---

### 残作業①：EPNモデルの移植

**最も重要で、最も難しい作業です。**

#### EPNとは何か（復習）

EPN（Error Propagation Network）は「患者間の類似度を使って予測を補正するモデル」です。

```
[入力] MLPが計算した各患者の生存予測 + 患者の特徴量
    ↓
[処理] 「似ている患者同士」をつなぐ注意マップを計算
    ↓
[出力] 周囲の患者の情報を借りて補正された予測
```

#### 問題：EPNの入力形式がValentinのフレームワークと合わない

彼女のEPN（`surv_epn/functions/epn.py`）は
**pandasのDataFrame**を入力として受け取ります。

```python
# EPN_surv の forward() の入力（epn.py 406行目付近）
def forward(self, input_db):
    # input_db は pandasのDataFrameで、以下の列を持つ:
    #   "0.5", "1.0", "2.3", ...  ← MLPの生存予測（列名が時間点の数値）
    #   "feat_0", "feat_1", ...   ← 患者の特徴量
    #   "label_duration"          ← 生存期間
    #   "label_event"             ← イベント発生フラグ
    #   "split"                   ← "train"/"val"/"test"の区別
    #   "pat_id"                  ← 患者ID
```

このDataFrameは、MLPが訓練後にCSVファイルとして保存したもの
（`loops_MLPclinmodel_main.py` の433行目付近）を読み込んだものです。

```
[MLPを訓練] → [結果をCSVファイルに保存] → [EPNがCSVを読み込んで訓練]
```

#### Valentinのフレームワークでの対応方法

ValentinのフレームワークではデータをPyTorch Geometricの`Data`オブジェクトで
扱うため、EPNの入力形式を変換する必要があります。

**やるべきこと（新規作成が必要なファイル）：**

```
① EPNのDataModuleを作る
   - MLPが出力したCSVファイルを読み込む
   - PyTorch Geometric の Data オブジェクトに変換する
   新ファイル: src/survival_analysis/data/epn_datamodule.py

② EPNをLightningModuleとしてラップする
   - EPN_surv クラスの核心部分を移植する
   新ファイル: src/survival_analysis/models/epn_module.py

③ EPNの設定ファイルを追加する
   新ファイル: configs/model/epn.yaml
```

**epn_module.py のイメージ（どんなコードになるか）：**

```python
# src/survival_analysis/models/epn_module.py（新規作成）
import lightning as pl
import torch.nn as nn
from pycox.models.loss import CoxCCLoss

class EPNModule(pl.LightningModule):
    def __init__(self, in_features, hidden_channels, optimizer):
        super().__init__()
        # surv_epn の EPN_surv の核心部分を移植
        # （患者間の注意マップを計算する層）
        self._key_projection = nn.Linear(in_features, in_features)
        self._query_projection = nn.Linear(in_features, in_features)
        self.loss_func = CoxCCLoss(shrink=0.)

    def training_step(self, batch, batch_idx):
        # batch.x = MLPの予測値 + 特徴量
        # batch.y = [生存期間, イベント]
        pred = self(batch.x)
        loss = self.loss_func(pred, batch.y[...,0], batch.y[...,1])
        return loss
```

---

### 残作業②：複数スプリット実験フレームワーク

#### 現状の違い

| | 彼女のコード | Valentinのパイプライン（現状） |
|---|---|---|
| テスト分割 | 5回繰り返し | **1回だけ**（`split_index=0`） |
| 学習ループ | 10回繰り返し | **なし** |
| 最終的な結果 | 5×10の平均値を報告 | 1回の結果だけ |

つまり彼女のコードは「5回×10回=50回」実験して平均を出しますが、
Valentinのパイプラインは今のところ1回しか実験しません。

#### やるべきこと

```
① splits.json に複数のスプリットを追加する
   （現在のsplits.jsonはすでにリスト形式なので対応しやすい）

② 全スプリットを自動的に実行するラッパースクリプトを作る
   新ファイル: src/survival_analysis/experiments/run_all_splits.py

③ 結果を集計して平均値・標準偏差を計算する
```

**ラッパースクリプトのイメージ：**

```python
# src/survival_analysis/experiments/run_all_splits.py（新規作成）
all_cindices = []
all_briers = []

for split_index in range(len(all_splits)):  # 全スプリットをループ
    datamodule = MetabricGraphSurvivalDataModule(
        json_splits_path=...,
        split_index=split_index   # ← ここを変えるだけ
    )
    # ... 訓練と評価 ...
    all_cindices.append(mean_cindex)
    all_briers.append(mean_brier)

# 全スプリットの平均を報告
print(f"C-index: {np.mean(all_cindices):.4f} ± {np.std(all_cindices):.4f}")
print(f"IBS:     {np.mean(all_briers):.4f} ± {np.std(all_briers):.4f}")
```

---

### 残作業③：損失関数の追加（優先度：低）

#### 現状

Valentinのパイプラインは **CoxPH損失関数**のみ対応しています。
彼女のコードはさらにCoxTimeとDeepHitSにも対応していますが、
**まずはCoxPHだけで移植を進め、必要があれば後から追加**するのが現実的です。

| 損失関数 | 彼女のコード | Valentinのパイプライン |
|---|---|---|
| CoxPH (DeepSurv) | 対応 | 対応済み |
| CoxTime | 対応 | 未対応 |
| DeepHitS | 対応 | 未対応 |

---

## 3. 移植の優先順位と進め方

### 優先順位

```
高 ① 複数スプリット実験フレームワーク（残作業②）
      → 比較的簡単。実験の信頼性に直結する

中 ② EPNモデルの移植（残作業①）
      → 最も重要。入力形式の変換が必要で難しい

低 ③ DeepHitS損失関数（残作業③）
      → まずはCoxPHで動かし、必要なら追加
```

### 推奨する進め方（ステップバイステップ）

```
Step 1: 現在のValentinのパイプラインをそのまま動かして数値を確認する
        → SurvivalDGM の C-index と IBS を記録する

Step 2: splits.json に複数スプリットを追加し、
        ラッパースクリプトを作って平均値を出す

Step 3: EPNのDataModuleを作る
        → MLPのCSV出力をEPNが受け取れる Data オブジェクトに変換

Step 4: EPNをLightningModuleとしてラップする
        → EPN_surv の核心部分をValentinスタイルに書き直す

Step 5: SurvivalDGM vs EPN の性能を比較する
```

---

## 4. 新規作成が必要なファイル一覧

| ファイルパス | 役割 | 難易度 |
|---|---|---|
| `src/survival_analysis/experiments/run_all_splits.py` | 全スプリットを実行するラッパー | 低 |
| `src/survival_analysis/data/epn_datamodule.py` | EPNの入力データを準備するDataModule | 高 |
| `src/survival_analysis/models/epn_module.py` | EPNをLightningModuleとしてラップ | 高 |
| `configs/model/epn.yaml` | EPNの設定ファイル | 低 |

---

## 5. 技術的な注意点：EPNの入力形式変換が最大の難関

彼女のEPNは**pandasのDataFrame**（CSV）を入力として期待していますが、
Valentinのフレームワークは**PyTorch Geometricの Data オブジェクト**を使います。

```python
# 彼女のコード：EPNに渡すDataFrameの列（epn.py 432〜444行目付近）
# "0.5", "1.2", "2.8", ...   ← MLPが予測した各時間点での生存確率（列名が数値）
# "feat_0", "feat_1", ...    ← 患者の特徴量
# "label_duration"           ← 実際の生存期間
# "label_event"              ← イベント発生フラグ（1=死亡, 0=打ち切り）
# "split"                    ← "train"/"val"/"test"
# "pat_id"                   ← 患者ID

# Valentinのフレームワーク：Data オブジェクトの構造
Data(
    x = tensor,          # 特徴量 (患者数 × 特徴量数)
    y = tensor,          # ラベル (患者数 × 2) ← [生存期間, イベント]
    edge_index = tensor  # エッジ情報
)
```

**解決策の選択肢：**

- **シンプルな方法**: MLPの予測値を `x` の一部として連結して `Data` に入れる。
  EPNのDataModuleで「CSV → テンソル → Data」の変換を行う。

- **忠実な移植**: EPNの `forward()` をDataFrameではなくテンソルで動くように書き直す。
  より大きな改修になるが、Valentinのスタイルに沿ったきれいなコードになる。

---

## まとめ

**完了済み：** フレームワーク全体（Lightning・Hydra）、GAT・MLP・SurvivalDGM、データモジュール（METABRIC・Gauthier）

**残り作業：**

| # | 作業 | 難易度 | 優先度 |
|---|---|---|---|
| ① | 複数スプリット実験ラッパー | 低 | 高 |
| ② | EPNモデルの移植 | 高 | 高 |
| ③ | DeepHitS損失関数の追加 | 中 | 低 |

---

## 参照ファイル

| 役割 | ファイル |
|---|---|
| 移植元：EPNモデル（最重要） | [surv_epn/functions/epn.py](surv_epn/functions/epn.py) |
| 参考：移植済みGATモデル | [Vanlentin's pipeline/src/survival_analysis/models/graph_module.py](Vanlentin's%20pipeline/src/survival_analysis/models/graph_module.py) |
| 参考：SurvivalDGM | [Vanlentin's pipeline/src/survival_analysis/models/dgm_model.py](Vanlentin's%20pipeline/src/survival_analysis/models/dgm_model.py) |
| 参考：データモジュール | [Vanlentin's pipeline/src/survival_analysis/data/metabric_datamodule.py](Vanlentin's%20pipeline/src/survival_analysis/data/metabric_datamodule.py) |
| 参考：評価スクリプト | [Vanlentin's pipeline/src/survival_analysis/experiments/evaluate_dgm.py](Vanlentin's%20pipeline/src/survival_analysis/experiments/evaluate_dgm.py) |
| MLPのCSV保存箇所 | [surv_epn/main/loops_MLPclinmodel_main.py](surv_epn/main/loops_MLPclinmodel_main.py)（433行目付近） |
