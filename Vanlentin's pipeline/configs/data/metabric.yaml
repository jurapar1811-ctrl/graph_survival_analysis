split_index: 0
json_splits_path: "${paths.data_dir}metabric/splits.json"

datamodule:
  _target_: survival_analysis.data.metabric_datamodule.MetabricGraphSurvivalDataModule
  json_splits_path: ${data.json_splits_path}
  split_index: ${data.split_index}
